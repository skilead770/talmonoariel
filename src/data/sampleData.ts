import { BarMitzvahRecord } from '../types';

export const SAMPLE_CSV_CONTENT = `פרשה,שם הנער,תאריך,הערות`;

export const parseSampleData = (): BarMitzvahRecord[] => {
  // Return empty array - all data comes from Firebase or user input
  return [];
};
